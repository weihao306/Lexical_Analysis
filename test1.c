#inuclude<stdio.h>
int main(void)
{
    int flower=ak;
    float 23err=your;

    while(your!=cross)
    {
        your=switch*ak;
        struct=tool+static+sizeof;
    }

    int num;
    scanf("%d",&num);
    int result= num*flower+23err;
    printf("%d",result);

    return 0;
}
